---
description: Push changes to GitHub and deploy to Netlify
---

1. Stage all changes
   `git add .`

2. Commit changes
   `git commit -m "Update code"`

3. Push to GitHub
   `git push`

4. Deploy to Netlify
   `netlify deploy --prod`
